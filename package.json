{
  "name": "neuroos",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroOS — an OS for brain-computer interfaces",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.5",
    "@types/node": "^20.12.0"
  },
  "packageManager": "pnpm@9.1.0"
}
