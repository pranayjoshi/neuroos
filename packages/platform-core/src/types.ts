/**
 * Platform Core public type re-exports.
 *
 * Job 04 — Platform Architect agent will expand this file.
 * Types from shared-contracts are re-exported here for SDK consumers.
 */

export type {
  RawSignalFrame,
  FeatureVector,
  IntentEvent,
  IntentEventSummary,
  SessionMetadata,
  DeviceAdapter,
  DeviceInfo,
  DeviceState,
  DeviceAdapterError,
} from "@neuroos/shared-contracts/schema";
