# NeuroOS DSP Pipeline — Job 02
# Implemented by the DSP Engineer agent. This is a stub.
FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir numpy scipy

COPY src/ ./src/

CMD ["python", "-c", "print('DSP Pipeline stub — see jobs/02_dsp_pipeline/JOB.md')"]
